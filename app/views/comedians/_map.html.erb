<% if @comedian.address.present? %>
<p><span>Location: </span><%= @comedian.address %></p>
  <div id="map"
       style="width: 100%; height: 150px;"
       data-markers="<%= @markers.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
<% end %>
